# Python
__pycache__/
*.py[cod]
*.so
.pytest_cache/

# Virtual environment
venv/
.venv/
env/

# Secrets & environment
.env
.env.*
!.env.example

# Flask instance folder (SQLite dev db)
instance/

# Artefak debugging (cookie sesi, dump HTML)
backup/

# Dump database: berisi hash password & data distribusi asli
*.sql

# Stub yang dibuat & ditimpa otomatis oleh cPanel Setup Python App
passenger_wsgi.py

# Artefak model: ditulis ulang aplikasi setiap pelatihan, jadi ini state
# runtime milik tiap lingkungan, bukan source code. Kalau hilang, aplikasi
# melatih ulang secara otomatis (lihat ForecastService).
lpg_forecast_app/app/models/collab/*.joblib
lpg_forecast_app/app/models/collab/models_metadata.json

# OS / editor
Thumbs.db
.DS_Store
.idea/
.vscode/
